const PRIMARY = {
    lighter: "#555",
    light: "#444",
    main: "#333",
    dark: "#222",
    darker: "#111",
    contrastText: "#000",
};

const palette = {
    mode: "dark",
    common: { black: "#000", white: "#fff" },
    primary: { ...PRIMARY },
    background: { paper: "#ffff", default: "#00000000" },
};

export default palette;
